import { EXTENSION_STEP, } from "../../../web-proof-commons/index.js";
export const notarize = (url, method = "GET", label, redact, outputs) => {
    return {
        url,
        method,
        label,
        redact: redact ?? [],
        outputs: outputs ?? [],
        step: EXTENSION_STEP.notarize,
    };
};
//# sourceMappingURL=notarize.js.map