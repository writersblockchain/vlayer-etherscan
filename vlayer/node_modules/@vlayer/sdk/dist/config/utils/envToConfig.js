import { keysToCamelCase } from "./camelCase.js";
import { match } from "ts-pattern";
import { DEFAULT_TESTNET_GAS_LIMIT, DEFAULT_MAINNET_GAS_LIMIT, } from "../../constants.js";
export const envToConfig = (config) => {
    const { examplesTestPrivateKey, vlayerApiToken, shouldDeployVerifierRouter, gasLimit, vlayerEnv, ...unchangedKeys } = keysToCamelCase(config);
    return {
        ...unchangedKeys,
        vlayerEnv,
        privateKey: examplesTestPrivateKey,
        token: vlayerApiToken,
        deployConfig: {
            shouldRedeployVerifierRouter: shouldDeployVerifierRouter ?? false,
        },
        gasLimit: gasLimit ??
            match(vlayerEnv)
                .with("testnet", "dev", () => DEFAULT_TESTNET_GAS_LIMIT)
                .with("mainnet", () => DEFAULT_MAINNET_GAS_LIMIT)
                .exhaustive(),
    };
};
//# sourceMappingURL=envToConfig.js.map